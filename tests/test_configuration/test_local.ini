#
#   !!! NOTE: this will be copied into `data_testing/test_local.ini` by the test suite
#

[app:main]
use = config:test.ini#main
sqlalchemy.url = sqlite:///%(here)s/ssl_minnow_local.sqlite
data_dir = %(here)s

[server:main]
use = egg:waitress#main
host = 127.0.0.1
port = 5002
